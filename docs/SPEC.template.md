# SPEC: <Название игры>

> Копия этого шаблона кладётся в `games/<slug>/SPEC.md` и заполняется ПЕРЕД кодингом.
> Claude Code читает SPEC.md и делает по нему ребрендинг + финтех-слой (шаги 2–3 пайплайна).
> Всё, что не указано здесь, агент выбирает по умолчанию из CLAUDE.md — минимизируйте неоднозначность.

## 1. Идентификация

- **slug** (папка / gameId): `<match3 | quiz | runner | wheel | ...>`
- **Рабочее название:**
- **Механика в одном предложении:**
- **Источник-шаблон:** <URL репозитория или CodeCanyon item> — см. запись в `docs/LICENSES.md`
- **Лицензия:** <MIT | CC0 | CodeCanyon Regular | ...> (обязательно совпадает с реестром)

## 2. Механика

- **Цель игрока:**
- **Управление:** <тач-свайпы | тап | drag; обязательно mobile-first, portrait>
- **Длительность партии:** <30–90 сек, целевое>
- **Как считается score:** <формула/правила — нужно для серверной валидации>
- **Максимально возможный score:** <для антифрод-лимита на сервере>
- **Прогресс/уровни:** <есть/нет, как>

## 3. Бренд и локализация

- **Палитра:** primary / secondary / background (из брендбука клиента; передаётся и через `theme` в INIT)
- **Шрифты:**
- **Маскот / ключевой арт:** <Kenney | CraftPix | AI-генерация>
- **Языки:** RU / UZ (+ EN?) — все строки через словарь локализации, без хардкода
- **Сплэш-экран:** <лого клиента, что показываем>

## 4. Финтех-слой (события моста)

Отметить, какие события игра шлёт (из `@gamewingo/game-bridge`):

- [ ] `GAME_READY` — при загрузке (обязательно)
- [ ] `GAME_START` — при нажатии Play (обязательно, нужен для лимита времени)
- [ ] `GAME_OVER` — в конце партии со `score` и `durationMs` (обязательно)
- [ ] `REWARD_CLAIM` — если есть механика забора награды
- [ ] `GAME_EVENT` — аналитика: <перечислить какие>

- **Баллы/награды:** <что и за что; финальное решение — сервер>
- **Лидерборд:** <есть/нет; глобальный или по программе лояльности>
- **Стрики:** <есть/нет>

## 5. Ограничения (проверить перед сдачей)

- [ ] Нет реальных денег и «выводов» — только виртуальные баллы/кэшбэк (App Store 4.7)
- [ ] Вес билда < 5 МБ (цель < 2 МБ)
- [ ] Загрузка < 3 сек на 3G, стабильные 60 fps на бюджетном Android
- [ ] Score валидируется на сервере (лимиты времени, max score, частота наград)
- [ ] Реклама/сторонняя аналитика из шаблона удалены

## 6. Definition of Done

- [ ] Игра проходит `docs/` чеклист WebView-QA (скилл `webview-qa`)
- [ ] Лицензия зафиксирована в `docs/LICENSES.md`
- [ ] Билд деплоится на Vercel и попадает в манифест каталога
- [ ] Экран «Об игре» с указанием авторства ассетов, где требует лицензия (CC-BY)
