{
  "name": "gamewingo",
  "version": "0.1.0",
  "private": true,
  "description": "Каталог встраиваемых HTML5-игр для финтех-приложения (Phaser 4 + TS + Vite)",
  "workspaces": [
    "packages/*",
    "games/*"
  ],
  "scripts": {
    "bridge:build": "npm run build -w @gamewingo/game-bridge",
    "build:games": "npm run build -w @gamewingo/soz -w @gamewingo/pairs -w @gamewingo/fifteen -w @gamewingo/2048 -w @gamewingo/sudoku-kids",
    "build:all": "npm run bridge:build && npm run build:games && node scripts/build-catalog.mjs",
    "new-game": "echo \"Скопируйте template-vite-ts-main в games/<name> — или используйте скилл new-game в Claude Code\"",
    "typecheck": "npm run typecheck --workspaces --if-present"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  },
  "engines": {
    "node": ">=20"
  }
}
